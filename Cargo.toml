[workspace]

resolver = "2"

members = ["contract-bindings", "contracts/rust", "sequencer", "utils"]

[workspace.dependencies]
jf-plonk = { path = "../jellyfish/plonk", features = ["test-apis", "test-srs"] }
jf-primitives = { path = "../jellyfish/primitives", features = [
  "std",
  "test-srs",
] }
jf-relation = { path = "../jellyfish/relation", features = ["std"] }
jf-utils = { path = "../jellyfish/utilities" }
