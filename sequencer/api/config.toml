[route.hotshot]
PATH = ["/hotshot"]
METHOD = "GET"
DOC = "Get the Hotshot configuration for the current node."

[route.env]
PATH = ["/env"]
METHOD = "GET"
DOC = "Get all ESPRESSO environment variables set for the current node."

[route.api_version]
PATH = ["/api_version"]
METHOD = "GET"
DOC = "Get the API version for the current node."

[route.chain_config]
PATH = ["/chain_config"]
METHOD = "GET"
DOC = "Get the CHAIN_CONFIG for the current node node."


